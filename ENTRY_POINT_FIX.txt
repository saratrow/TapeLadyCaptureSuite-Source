Milestone 2 entry-point correction
==================================

The project now explicitly identifies TapeLadyCaptureSuite.Program as its
startup object and explicitly includes all C# source files in compilation.

Open the corrected .csproj, choose Build > Clean Solution, then
Build > Rebuild Solution.
